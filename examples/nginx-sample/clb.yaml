# CLB variant. NOT supported on EKS Auto Mode (stays <pending>); use on a
# classic cluster (e.g. cluster-minimal.yaml) only. Replaces service.yaml +
# alb.yaml — delete those first (see steps in nlb.yaml).
# ---
# apiVersion: v1
# kind: Service
# metadata:
#   labels:
#     app: nginx
#   name: nginx-service
#   namespace: default
# spec:
#   ports:
#   - name: http
#     port: 80
#     targetPort: 80
#   selector:
#     app: nginx
#   type: LoadBalancer
